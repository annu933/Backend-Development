<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          href="/logout"
          class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block"
          >Logout</a
        >
      </div>
      <h3 class="text-3xl mb-5">
        <span class="font-light">Hello, <%= user.name %></span>
      </h3>
      <h5 class="mb-2">You can create a post.</h5>
      <form action="/post" method="post">
        <textarea
          class="block w-1/3 p-3 outline-none bg-transparent resize-none border-2 border-zinc-800 rounded-md"
          placeholder="What's on your mind ?"
          name="content"
        ></textarea>
        <input
          class="px-3 w-40 py-2 mt-5 bg-blue-500 block rounded-md text-sm"
          type="submit"
          value="Create new post"
        />
      </form>
      <div class="posts">
        <h3 class="text-zinc-400 mt-20">Your Posts.</h3>
        <div class="postcontainer mt-2">

          <%
          user.posts.reverse().forEach(function(item){ %>
            <div
            class="post w-1/3 p-4 mb-4 border-2 bg-zinc-800 border-[1px] border-zinc-700 rounded-md"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tight">
             <%= item.content %>
            </p>
            <div class="btns flex mt-5 gap-4">
              <a class="text-blue-500" href="">Like</a>
              <a class="text-zinc-400" href="">Edit</a>
            </div>
          </div>
        <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
