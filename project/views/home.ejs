<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    </link>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <h1>Home Page</h1>
        <p>We are using ejs, that will give you solution for calculate any thing in your html code</p>
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none" type="text"
                    placeholder="Title" name="title" />
                <textarea class="block mt-2 px-5 py-2 bg-zinc-800 resize-none w-full rounded-md outline-none"
                    placeholder="Write your task details here" name="details"></textarea>
                <input type="submit" value="Create task" class="mt-2 bg-blue-600 px-5 py-2 rounded-md" />
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10">
            <% if(files.length> 0){ %>
                <% files.forEach(function(val){ %>

                    <div class="task w-72 px-3 py-4 bg-zinc-800 p-10 rounded-md">
                        <h1 class="text-white text-2xl tracking-tighter">
                            <%= val %>
                        </h1>
                        <div class="flex w-full justify-between items-center mt-3">
                            <a href="/files/<%= val %>" class="text-blue-500 inline-block">Read More</a>
                            <a href="/edit/<%= val %>" class="text-zinc-500 inline-block">Edit File Name</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600">No task yet.</h3>

                            <% } %>



        </div>
    </div>

    <span>sum of 3 + 4 = <%= 3+4 %></span>
    <script src="/javascripts/script.js"></script>
</body>

</html>